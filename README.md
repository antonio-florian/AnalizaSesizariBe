# AnalizaSesizariBe

- Back-end of the application

-To start the server on port 3000 with nodemon (wich updates the server to display the latest changes) type in terminal:
-npx nodemon server.js
-Navigate to `http://localhost:3000/`.